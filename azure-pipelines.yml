trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
- group: New variable group 27-Jul

- name: solution
  value: '**/*.sln'

- name: buildPlatform
  value: 'Any CPU'

- name: buildConfiguration
  value: 'Release'

steps:

# ---------------------------------------------------------
# Restore
# ---------------------------------------------------------
- task: DotNetCoreCLI@2
  displayName: Restore packages
  inputs:
    command: 'restore'
    projects: '$(solution)'

# ---------------------------------------------------------
# Build
# ---------------------------------------------------------
- task: DotNetCoreCLI@2
  displayName: Build solution
  inputs:
    command: 'build'
    projects: '$(solution)'
    arguments: '--configuration $(buildConfiguration)'

# ---------------------------------------------------------
# Test + coverage
# ---------------------------------------------------------
- task: DotNetCoreCLI@2
  displayName: Run tests
  inputs:
    command: 'test'
    projects: '$(solution)'
    arguments: '--configuration $(buildConfiguration) --collect "XPlat Code Coverage"'

# ---------------------------------------------------------
# Publish coverage
# ---------------------------------------------------------
- task: PublishCodeCoverageResults@1
  displayName: Publish coverage
  inputs:
    codeCoverageTool: 'Cobertura'
    summaryFileLocation: '$(Agent.TempDirectory)/**/coverage.cobertura.xml'
    reportDirectory: '$(Agent.TempDirectory)'

# ---------------------------------------------------------
# Publish app
# ---------------------------------------------------------
- task: DotNetCoreCLI@2
  displayName: Publish app
  inputs:
    command: 'publish'
    publishWebProjects: true
    arguments: '--configuration Release --output $(build.artifactStagingDirectory)'
    zipAfterPublish: true

# ---------------------------------------------------------
# Build Docker image
# ---------------------------------------------------------
- task: Docker@2
  displayName: Build Docker image
  inputs:
    command: build
    Dockerfile: SampleApi/Dockerfile
    tags: |
      ghcr.io/mongolia144/sampleapi:$(Build.BuildId)

# ---------------------------------------------------------
# Login to GHCR
# ---------------------------------------------------------
- script: |
    echo $(GHCR_PAT) | docker login ghcr.io -u mongolia144 --password-stdin
  displayName: Login to GHCR

# ---------------------------------------------------------
# Push Docker image
# ---------------------------------------------------------
- task: Docker@2
  displayName: Push Docker image
  inputs:
    command: push
    tags: |
      ghcr.io/mongolia144/sampleapi:$(Build.BuildId)

# ---------------------------------------------------------
# ⭐ Deploy to Azure Container Apps (recommended method)
# ---------------------------------------------------------
- task: AzureContainerAppsDeploy@1
  displayName: Deploy to Azure Container Apps
  inputs:
    azureSubscription: Azure subscription 1
    resourceGroup: sampleApi_AzureDevOps_Acr_rg
    containerAppName: sampleapi
    imageToDeploy: ghcr.io/mongolia144/sampleapi:$(Build.BuildId)
    containerRegistryServer: ghcr.io
    containerRegistryUsername: mongolia144
    containerRegistryPassword: $(GHCR_PAT)
