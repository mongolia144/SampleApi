trigger:
- main

pool:
  vmImage: 'ubuntu-latest'   # REQUIRED: Linux agent so Docker can build Linux containers

variables:
- group: New variable group 27-Jul

- name: solution
  value: '**/*.sln'

- name: buildPlatform
  value: 'Any CPU'

- name: buildConfiguration
  value: 'Release'

steps:

# ---------------------------------------------------------
# Restore using dotnet (Linux-compatible)
# ---------------------------------------------------------
- task: DotNetCoreCLI@2
  displayName: Restore packages
  inputs:
    command: 'restore'
    projects: '$(solution)'

# ---------------------------------------------------------
# Build using dotnet (Linux-compatible)
# ---------------------------------------------------------
- task: DotNetCoreCLI@2
  displayName: Build solution
  inputs:
    command: 'build'
    projects: '$(solution)'
    arguments: '--configuration $(buildConfiguration)'

# ---------------------------------------------------------
# Test + coverage
# ---------------------------------------------------------
- task: DotNetCoreCLI@2
  displayName: Run tests
  inputs:
    command: 'test'
    projects: '$(solution)'
    arguments: '--configuration $(buildConfiguration) --collect "XPlat Code Coverage"'

# ---------------------------------------------------------
# Publish coverage results
# ---------------------------------------------------------
- task: PublishCodeCoverageResults@1
  displayName: Publish coverage
  inputs:
    codeCoverageTool: 'Cobertura'
    summaryFileLocation: '$(Agent.TempDirectory)/**/coverage.cobertura.xml'
    reportDirectory: '$(Agent.TempDirectory)'

# ---------------------------------------------------------
# Publish ASP.NET Core app (Linux-ready)
# ---------------------------------------------------------
- task: DotNetCoreCLI@2
  displayName: Publish app
  inputs:
    command: 'publish'
    publishWebProjects: true
    arguments: '--configuration Release --output $(build.artifactStagingDirectory)'
    zipAfterPublish: true

# =====================================================================
# EVERYTHING BELOW THIS LINE IS COMMENTED OUT (DEPLOYMENT DISABLED)
# =====================================================================

# ---------------------------------------------------------
# Build Docker image for GHCR (DISABLED)
# ---------------------------------------------------------
# - task: Docker@2
#   displayName: Build Docker image
#   inputs:
#     command: build
#     Dockerfile: SampleApi/Dockerfile
#     tags: |
#       ghcr.io/mongolia144/sampleapi:$(Build.BuildId)

# ---------------------------------------------------------
# Login to GHCR (DISABLED)
# ---------------------------------------------------------
# - script: |
#     echo $(GHCR_PAT) | docker login ghcr.io -u mongolia144 --password-stdin
#   displayName: Login to GHCR

# ---------------------------------------------------------
# Push Docker image to GHCR (DISABLED)
# ---------------------------------------------------------
# - task: Docker@2
#   displayName: Push Docker image
#   inputs:
#     command: push
#     tags: |
#       ghcr.io/mongolia144/sampleapi:$(Build.BuildId)

# ---------------------------------------------------------
# Deploy container to Azure Container Apps (DISABLED)
# ---------------------------------------------------------
# - task: AzureCLI@2
#   displayName: Deploy to Azure Container Apps
#   inputs:
#     azureSubscription: Azure subscription 1(c13b401c-dde4-4298-ad6c-3ef836f847d5)
#     scriptType: bash
#     scriptLocation: inlineScript
#     inlineScript: |
#       az containerapp registry set \
#         --name sampleapi \
#         --resource-group sampleApi_AzureDevOps_Acr_rg \
#         --server ghcr.io \
#         --username mongolia144 \
#         --password "$GHCR_PAT"
#
#       az containerapp update \
#         --name sampleapi \
#         --resource-group sampleApi_AzureDevOps_Acr_rg \
#         --image ghcr.io/mongolia144/sampleapi:$(Build.BuildId)
